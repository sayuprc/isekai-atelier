import { defineConfig } from 'astro/config'

// https://astro.build/config
export default defineConfig({
  base: '/isekai-atelier',
  site: 'https://jules.example.com', // Placeholder, though not strictly required unless using sitemap
})
